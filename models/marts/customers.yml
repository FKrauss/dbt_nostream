version: 2

models:
  - name: customers
    description: "Customer mart model - update with actual implementation"
    columns:
      - name: customer_id
        description: "Customer identifier"
        tests:
          - unique
          - not_null